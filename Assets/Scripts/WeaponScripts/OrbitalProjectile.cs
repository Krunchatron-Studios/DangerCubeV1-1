using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class OrbitalProjectile : Projectile {
    // public float startingLength = 2;
    // public float beamSize;
    //
    // void Awake() {
    //     beamSize = GetComponent<Transform>().localScale.y;
    //     BeamGrow(targetPosition);
    // }
    //
    // public void BeamGrow(Vector3 targetPosition) {
    //     // distance is generating a distance
    //     Vector3 destination = targetPosition;
    //     float difference = Vector3.Distance(transform.position, destination);
    //     Debug.Log("This is the distance: " + difference);
    //     beamSize = difference;
    // }
}
